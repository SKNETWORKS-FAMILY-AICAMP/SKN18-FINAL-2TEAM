########################################
# Worker 전용 requirements.txt
# RabbitMQ Consumer 및 시뮬레이션 작업에 필요한 최소 패키지만 포함
########################################

# Django (ORM 사용)
Django>=5.0,<6.0
django-environ

# Database
psycopg[binary, pool]

# RabbitMQ
pika

# HTTP 요청 (RunPod API)
requests

# 환경변수 관리
python-dotenv

# 설정 파일 (YAML)
PyYAML

# AWS SDK (Parameter Store 접근)
boto3

# 기본 유틸리티
python-dateutil

