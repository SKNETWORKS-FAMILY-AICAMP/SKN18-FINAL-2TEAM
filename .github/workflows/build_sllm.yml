name: Build sLLM (placeholder)

on:
  # 초기 단계: 수동으로만 실행
  workflow_dispatch:

jobs:
  build_sllm_placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Show placeholder message
        run: |
          echo "build_sllm.yml placeholder workflow"
          echo "👉 sLLM Docker build / finetune pipeline은 아직 구현 전입니다."
          echo "👉 나중에 vLLM/Ollama 이미지 빌드 스텝으로 교체하세요."
