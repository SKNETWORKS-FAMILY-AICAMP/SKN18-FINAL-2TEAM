########################################
# 로컬 개발 환경용 requirements.txt
# Lambda 배포는 별도 requirements-lambda-*.txt 파일 사용
########################################

# 기본 패키지
numpy==1.26.4
# PyTorch는 Dockerfile에서 CPU-only 버전으로 별도 설치 (크기 절감: 2GB → 500MB)
# torch==2.2.2  # Dockerfile에서 먼저 설치하므로 여기서는 제외
pandas
scipy
spacy
gilda

# OpenAI API (embedding용)
openai
FlagEmbedding
# LangChain (필요시)
langchain
langchain-core
langchain-community
langchain-openai
langchain_text_splitters
tiktoken
langchain_huggingface

# LangGraph (워크플로우 오케스트레이션)
langgraph
dateparser # 날짜 파싱 - 리라이트쿼리 tool

# Database
psycopg[binary, pool]
pgvector
sqlalchemy

# Neo4j (Knowledge Graph)
neo4j
neo4j-graphrag  # Neo4j TextRetriever
py2neo

# Django Web Framework
Django>=5.0,<6.0
django-environ
djangorestframework
gunicorn
django-sass-processor
libsass

# ETL / Parsing
requests
lxml
markdownify
python-dateutil
beautifulsoup4
nltk

sentence-transformers
transformers
accelerate

# Simulation tools (optional, utils)
biopython

# 환경변수 관리
python-dotenv

# 기타 유틸리티
PyYAML

# Jupyter Notebook Support 제거 (서비스용 컨테이너이므로 불필요)
# 로컬 개발 시에만 필요하면 별도로 설치: pip install jupyter notebook ipywidgets
# jupyter
# ipywidgets
# notebook

# AWS SDK (Lambda에 기본 포함되지만 명시)
boto3

# RabbitMQ (Message Queue)
pika